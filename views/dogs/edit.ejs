<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= dog.name %></title>
  </head>
  <body>
    <h1>Edit <%= dog.name %></h1>
    <form action="/dogs/<%= dog._id %>?_method=PUT" method="POST">
      <label for="name">Name:</label>
      <input type="text" name="name" value="<%= dog.name %>" required />
      <label for="breed">Breed:</label>
      <input type="text" name="breed" value="<%= dog.breed %>" required />
      <label for="age">Age:</label>
      <input type="text" name="age" value="<%= dog.age %>" required />
      <label for="isAdopted">Is Adopted:</label>
      <input type="checkbox" name="isAdopted" <%= dog.isAdopted ? 'checked' : ''
      %>>
      <button type="submit">Update Dog</button>
    </form>
    <a href="/dogs/<%= dog._id %>">Cancel</a>
  </body>
</html>
